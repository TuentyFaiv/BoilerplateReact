import styled from "styled-components";
import { Link } from "react-router-dom";

import type { ScrollLinkI } from "@typing/styles";

export const ScrollLink = styled(Link)<ScrollLinkI>``;

export const other = {};
